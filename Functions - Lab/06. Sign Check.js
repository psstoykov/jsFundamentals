function solve(one, two, three) {

    (one * two * three > 0) ? console.log("Positive") : console.log("Negative");
};

solve(5,
    12,
    -15);
solve(-6,
    -12,
    14);
solve(-1,
    -2,
    -3);
solve(-5,
    1,
    1);