function solve(input, startIndex, count) {

    let endIndex = startIndex + count;
    let result;


    result = input.substring(startIndex, endIndex)

    console.log(result);

};

solve('ASentenceeeee', 1, 8);

solve('SkipWord', 4, 7);